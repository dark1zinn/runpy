[project]
name = "runpy-monorepo"
version = "0.1.0"

[tool.uv.workspace]
# This tells uv to look into these folders for sub-projects
members = ["worker", "examples/*/worker"]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "ruff>=0.15.8",
    "uv>=0.11.2",
]
